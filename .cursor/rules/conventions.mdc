---
description: Load docs/agent-rules.md as the project's single source of agent rules
alwaysApply: true
---

# Cursor

`docs/agent-rules.md` is the single source of truth for domain naming, style, security, the
attempt-lifecycle invariants, quality, git, and when to ask. Read it before your first edit in a session.

Do not restate those rules here. If this file and `docs/agent-rules.md` disagree, `docs/agent-rules.md`
wins.
