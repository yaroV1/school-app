<%= form_with model: subject, class: "card card-pad max-w-md space-y-4" do |f| %>
  <%= render "shared/form_errors", record: subject %>
  <div class="fieldset">
    <%= f.label :name, class: "field-label" %>
    <%= f.text_field :name, required: true, class: field_class %>
  </div>
  <%= f.button(class: btn_primary) { |label| safe_join([ ui_icon("check"), label ]) } %>
<% end %>
