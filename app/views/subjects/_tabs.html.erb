<nav class="tab-bar" aria-label="<%= t("subjects.show.tabs_label") %>">
  <%= tab_link_to t("subjects.show.tests"), subject_path(subject) %>
  <%= tab_link_to t("subjects.show.stats"), stats_subject_path(subject) %>
</nav>
