<% content_for :title, t("subjects.edit.title") %>
<div class="mb-6">
  <%= link_to t("subjects.show.back_to_subject"), subject_path(@subject), class: "text-sm text-slate-600 underline" %>
  <h1 class="mt-2 text-2xl font-semibold"><%= t("subjects.edit.title") %></h1>
</div>
<%= render "form", subject: @subject %>
