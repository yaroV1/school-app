<% if question.photo.attached? %>
  <div class="mb-3">
    <%= image_tag question.photo, class: "max-h-80 w-auto max-w-full rounded-control border border-line", alt: t("exams.show.photo_alt") %>
  </div>
<% end %>
