<%# span: extra classes for the wrapper, e.g. a grid column span. %>
<% if record.errors.any? %>
  <div class="field-error <%= local_assigns[:span] %>" role="alert"><%= record.errors.full_messages.to_sentence %></div>
<% end %>
